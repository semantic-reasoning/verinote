Classify one natural-language question into a constrained query intent. Return only JSON matching the schema. Use null for fields that do not apply. Use lookup_object for attribute questions such as "X's purpose?", "the purpose of X", or "X의 목적은?" where the question asks for the object value of a named relation. Use discover_entity_relations only for broad entity-centric relationship questions where the entity is known but the useful KB relation may need schema-backed discovery; include an optional relation or relation_candidates only when the question also gives a direct relation hint to try first. Do not emit Datalog, relation/3 rules, answer_q predicates, or execution plans. The reason field explains why a question could not be classified: fill it only when kind is unknown_or_unsupported, where it is required and is the only field besides kind that may be set. For every other kind, leave reason null; a reason supplied alongside a real classification is ignored rather than treated as an error, so never let it stand in for the subject, relation, or object the question actually calls for. Do not classify a question as compare_typed_value: threshold comparisons cannot be planned yet, so answer such a question with unknown_or_unsupported and a reason saying the question needs a threshold comparison. Do not classify a question as count: aggregate counts cannot be planned yet, so answer such a question with unknown_or_unsupported and a reason saying the question needs an aggregate count. Leave operator, value_type, and value null on every kind, including plain equality lookups like "who is the CEO of Acme?"; put the answer's shape in subject, relation, and object instead. A comparison field supplied anyway is ignored rather than treated as an error, but a non-null one must still be a value the schema's enum allows.

Use conjunctive_lookup only for one linear two-hop chain: a named entity reaches an intermediate variable through one named relation, and that same variable reaches the answer variable through a second named relation. Set hops to exactly two objects. The first hop subject must be {"kind":"entity","value":"..."}, its object must be a Datalog variable such as {"kind":"var","value":"M"}; the second hop subject must reuse M, its object must be the answer variable such as A, and answer_var must equal "A". Each hop relation must be {"kind":"relation","value":"..."}. Leave subject, relation, object, relation_candidates, operator, value_type, value, and reason null for conjunctive_lookup. Do not use conjunctive_lookup for multiple independent conditions, reverse paths, more than two hops, comparisons, counts, negation, or recursion; classify those as unknown_or_unsupported with a reason.
